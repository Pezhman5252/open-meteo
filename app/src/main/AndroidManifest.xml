<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        حذف صریح مجوزهای ممنوعه از مانیفست ترکیبی (manifest merge).
        کافه‌بازار این مجوزها را در اپ‌ها نمی‌پذیرد. این خط‌ها defensive هستند:
        اگر هیچ کتابخانه‌ای آن‌ها را اعلام نکند، بدون اثر می‌مانند؛ اگر در آینده
        کتابخانه‌ای اضافه‌شان کند، مانع merge به APK نهایی می‌شوند.
        (SDK پرداخت رسمی کافه‌بازار = Poolakey 2.2.0؛ SDK مایکت/Myket وجود ندارد.)
    -->
    <uses-permission android:name="ir.mservices.market.BILLING" tools:node="remove" />
    <uses-permission android:name="com.myket.market.permission.BILLING" tools:node="remove" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.SEND_SMS" tools:node="remove" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" tools:node="remove" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
